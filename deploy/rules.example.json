[
  {
    "name": "secret-read",
    "pattern": "^READ .*(secret|token|credential|password|shadow|wallet|\\.pem|\\.key|\\.aws|\\.ssh|\\.npmrc|\\.env|id_rsa)",
    "always_on": true,
    "action": "alert"
  },
  {
    "name": "cloud-metadata",
    "pattern": "^CONNECT 169\\.254\\.169\\.254:",
    "always_on": true,
    "action": "alert"
  },
  {
    "name": "new-outbound-connect",
    "pattern": "^CONNECT ",
    "exclude": [],
    "action": "alert"
  },
  {
    "name": "new-exec",
    "pattern": "^EXEC ",
    "exclude": [],
    "action": "warn",
    "_comment": "warn builds audit evidence (would_block); use block to compile kernel deny verdicts when enforcement is armed — see docs/enforcement.md"
  },
  {
    "name": "block-example",
    "pattern": "^READ /etc/shadow$",
    "always_on": true,
    "action": "block",
    "_comment": "example only — uncomment/adapt after enforce.enabled=true and goodman.io/enforce=enabled on target namespaces"
  }
]
